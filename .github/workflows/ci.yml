name: verify

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  certificates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and verify
        run: make check
